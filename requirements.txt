# O toolkit financeiro (financeiro/) usa apenas a biblioteca padrão do Python 3.11+.
# Nenhuma dependência é necessária para OFX, fluxo de caixa e conciliação.

# Opcional — leitura do relatório de cobrança do BB em .xls binário (consultaCBR).
# Só é necessário para o comando /conciliar-boletos com arquivo .xls; .xlsx lê sem isto.
xlrd>=2.0

# Opcional — apenas para a sincronização com o Notion (financeiro/notion_sync.py),
# que também funciona só com a stdlib via urllib. Descomente se preferir 'requests':
# requests>=2.31
